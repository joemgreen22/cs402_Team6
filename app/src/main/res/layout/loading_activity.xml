<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_screen"
    android:foregroundTint="#E1CECE"
    tools:context=".LoadingActivity">

    <TextView
        android:id="@+id/loadingSing"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/loadProgressBar"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp"
        android:text="Loading..."
        android:textColor="#FFFFFF"
        android:textSize="48dp"
        android:typeface="sans" />

    <ProgressBar
        android:id="@+id/loadProgressBar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:progressTint="#E11414" />

    <TextView
        android:id="@+id/loadProgressBarTxView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/loadProgressBar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="12dp"
        android:text="Loading Complete"
        android:textColor="#FF5722"
        android:textSize="40dp"
        android:textStyle="bold"
        android:visibility="invisible" />


</RelativeLayout>